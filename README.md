# opengl_shapes

Personal project for learning openGL and the strategy design pattern in C++. 

Draws different shapes both in an openGL window and the terminal, by using the "OpenGLDrawer" and "TerminalDrawer" classes as strategies when creating a shape.

Runs in Visual Studio 2022 selecting Release x86. GLEW and GLFW included in the /dependencies/ folder.
